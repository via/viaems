language:
os: linux
dist: xenial
compiler: gcc
addons: 
  apt:
    packages:
      - bmake
before_install:
  - "mkdir libcheck"
  - "wget https://github.com/libcheck/check/releases/download/0.12.0/check-0.12.0.tar.gz -O - | tar -xvz"
  - "cd libcheck-0.12.0; ./configure; make; sudo make install"

script:
  - cd src && bmake check && ./check
