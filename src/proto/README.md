This directory contains generated files for the console protobuf implementation.

These are generated using viapb, which requires python and the python
dependencies in py/requirements.txt.  To regenerate, run
```
make proto
```
